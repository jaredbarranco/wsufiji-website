{
  "$schema": "https://opencode.ai/config.json",
  "instructions": [
    "Use the supabase MCP against the locally running database on localhost:54321 to confirm current db schema prior to doing any database operations like: SELECT, INSERT, UPDATE, DELETE, or ALTERS, or when writing supabase migration files."
  ],
  "mcp": {
    "supabase": {
      "type": "remote",
      "url": "http://localhost:54321/mcp"
    }
  }
}
